*** Settings ***
Resource          ../ComLib/Flow.txt
Resource          Lib/flow.txt
Resource          Lib/element.txt

*** Test Cases ***
添加ORACLE数据源
    login
    Page Should Not Contain Button    ${btn_login}
    log    用户登录成功
    GotoConnection
    AddConnection    ORACLE
    sleep    1s
    InputConnectionInfo    ${DB.url}    ${DB.username}    ${DB.password}
    SaveConnection
